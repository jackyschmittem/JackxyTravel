import styled from "styled-components"
import theme from "../../../theme"

const HeaderStyled = styled.header`
  padding: 1rem 0;
  background: ${theme.bgHeader || "#c2c2c2"};
  color: #fff;
  margin: auto;
`

export default HeaderStyled
